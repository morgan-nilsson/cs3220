# These are the two locations for the two-state environment
loc_A, loc_B = (0, 0), (1, 0)

# for the Task3
# I had to change loc_c from 1,0 -> 0,1 to fix the CompanyEnviroment.execute_action method
loc_C, loc_D =(0, 1), (1, 1)